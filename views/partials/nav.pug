.nav_container 
    .nav_menu_container
        .nav_menu_box
            .nav_menu 
                a(href=`${routes.home}`)  Home
            .nav_menu 
                a(href=`${routes.data}${routes.map}`)  Map
            .nav_menu 
                a(href=`${routes.data}${routes.popol}`)  Portpolio
    .nav_login_container 
        .nav_toggle
            .nav_icon_box
                i.fas.fa-ellipsis-v
            .nav_dropdown_box
                .nav_dropdown.hidden  
                    .nav_login 
                        a(href=`${routes.login}`)  
                            span login 